{
  "name": "bsc-zkp",
  "version": "1.0.0",
  "description": "BSC zero-knowledge proof module — standalone, no blockchain/API dependency",
  "private": true,
  "scripts": {
    "setup": "docker compose run --rm zkp-setup",
    "prove": "node scripts/prove.js",
    "verify": "node scripts/verify.js",
    "test": "node tests/run-all.js"
  },
  "dependencies": {
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.3"
  },
  "devDependencies": {
    "circomlib": "^2.0.5"
  }
}
